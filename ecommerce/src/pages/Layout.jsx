import React from "react";
import { Header, NewProducts } from "../components";

const Layout = () => {
  return (
    <div>
      <Header />
      <NewProducts />
    </div>
  );
};

export default Layout;
